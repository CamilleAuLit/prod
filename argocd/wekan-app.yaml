apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: wekan
  namespace: argocd
spec:
  project: default
  source:
    repoURL: "https://github.com/CamilleAuLit/prod.git"
    targetRevision: HEAD
    # On pointe précisément vers le dossier du chart wekan
    path: app/wekan
    # On précise à ArgoCD que c'est un chart Helm
    helm:
      valueFiles:
        - values.yaml
  destination:
    server: "https://kubernetes.default.svc"
    namespace: wekan # Ou le namespace où tu veux déployer wekan
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
