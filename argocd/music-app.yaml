apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: music
  namespace: argocd
spec:
  project: default
  source:
    repoURL: 'https://github.com/CamilleAuLit/prod.git'
    targetRevision: HEAD
    # On pointe précisément vers le dossier du chart Music
    path: app/music
    # On précise à ArgoCD que c'est un chart Helm
    helm:
      valueFiles:
        - values.yaml
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: airsonic # Ou le namespace où tu veux déployer Music
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
